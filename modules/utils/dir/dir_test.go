package dir_test

// 注意，此测试用例只能在项目根目录执行，否则必崩。
// func TestDir(t *testing.T) {
// 	if d := dir.SourceRelativeDir(); d != `modules/utils/dir` {
// 		log.Println(os.Getwd())
// 		panic(`相对目录获取不正确：` + d)
// 	}
// }
